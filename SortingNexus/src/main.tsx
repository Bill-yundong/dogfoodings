import React from 'react';
import ReactDOM from 'react-dom/client';
import { SortingDashboard } from './components';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SortingDashboard />
  </React.StrictMode>
);
